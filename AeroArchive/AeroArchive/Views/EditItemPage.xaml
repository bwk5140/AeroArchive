<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AeroArchive.Views.EditItemPage"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Done" Command="{Binding DoneEditingCommand}" />
    </ContentPage.ToolbarItems>

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Product ID:" FontSize="Medium" />
        <Entry Text="{Binding Text}" FontSize="Small"/>
        <Label Text="Warranty Status:" FontSize="Medium" />
        <Entry Text="{Binding WarrantyStatus}" FontSize="Small"/>
        <Label Text="Description:" FontSize="Medium" />
        <Editor Text="{Binding Description}" FontSize="Small"/>
    </StackLayout>

</ContentPage>