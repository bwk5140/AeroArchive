<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AeroArchive.Views.RegistrationPage"
             xmlns:local="clr-namespace:AeroArchive.Utils"
             Shell.FlyoutBehavior="Disabled"
             Shell.NavBarIsVisible="False">

    <ContentPage.Resources>
        <ResourceDictionary Source="../Resources/Fonts/FontRD.xaml" />
        <ResourceDictionary Source="../Resources/Fonts/EntryValidationRD.xaml" />
        <StyleSheet Source="../Styles/RegistrationPage.css" />
    </ContentPage.Resources>

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <FlexLayout x:Name="Form" StyleClass="body">
            <StackLayout x:Name="RegistrationForm" StyleClass="container">
                <Label StyleClass="logo" Text="Create new account" TextColor="DodgerBlue" />
                <StackLayout StyleClass="registration-form"
                             Orientation="Vertical">

                    <Entry
                        x:Name="FirstNameEntry"
                        IsSpellCheckEnabled="false"
                        Placeholder="First Name"
                        HorizontalOptions="FillAndExpand"
                        PlaceholderColor="Gray"
                        Text="{Binding FirstName}"
                        TextColor="DarkSlateGray"
                        TextChanged="Handle_TextChanged"/>
                    <Entry
                        x:Name="LastNameEntry"
                        IsSpellCheckEnabled="false"
                        Placeholder="Last Name"
                        HorizontalOptions="FillAndExpand"
                        PlaceholderColor="Gray"
                        Text="{Binding LastName}"
                        TextColor="DarkSlateGray" 
                        TextChanged="Handle_TextChanged"/>
                
                    <Entry 
                       x:Name="UserNameEntry"
                       MaxLength="7"
                       Placeholder="Username"
                       PlaceholderColor="Gray"
                       Text="{Binding UserName}"
                       TextColor="DarkSlateGray"
                       TextChanged="Handle_TextChanged">
                    </Entry>

                    <Entry 
                       x:Name="EmailEntry"
                       Placeholder="Email"
                       PlaceholderColor="Gray"
                       Text="{Binding Email}"
                       TextColor="DarkSlateGray"
                       TextChanged="Handle_TextChanged">
                    </Entry>
                    <Grid>
                        <Entry
                            x:Name="PasswordEntry"
                            IsPassword="{Binding Source={x:Reference ShowPasswordActualTrigger}, Path= HidePassword}" 
                            Placeholder="Password"
                            PlaceholderColor="Gray"
                            Text="{Binding Password}"
                            TextColor="DarkSlateGray"
                            TextChanged="Handle_TextChanged">
                        </Entry>
                        <ImageButton VerticalOptions="Center"
                                 Margin="0,0,40,0"
                                 HeightRequest="20"
                                 HorizontalOptions="End"
                                 Source="iconEyeHide.png"
                                 Grid.Column="0">
                            <ImageButton.Triggers>
                                <EventTrigger Event="Clicked">
                                    <local:ShowPasswordTriggerAction ShowIcon="iconEyeShow.png"
                                                                 HideIcon="iconEyeHide.png"
                                                                 x:Name="ShowPasswordActualTrigger"/>
                                </EventTrigger>
                            </ImageButton.Triggers>
                        </ImageButton>
                    </Grid>
                    <Grid>
                        <Entry
                            x:Name="ConfirmPasswordEntry"
                            IsPassword="{Binding Source={x:Reference ShowPasswordActualTrigger1}, Path= HidePassword}" 
                            Placeholder="Confirm password"
                            PlaceholderColor="Gray"
                            Text="{Binding ConfirmPassword}"
                            TextColor="DarkSlateGray"
                            TextChanged="Handle_TextChanged">
                        </Entry>
                        <ImageButton VerticalOptions="Center"
                                 Margin="0,0,40,0"
                                 HeightRequest="20"
                                 HorizontalOptions="End"
                                 Source="iconEyeHide.png"
                                 Grid.Column="0">
                            <ImageButton.Triggers>
                                <EventTrigger Event="Clicked">
                                    <local:ShowPasswordTriggerAction ShowIcon="iconEyeShow.png"
                                                                 HideIcon="iconEyeHide.png"
                                                                 x:Name="ShowPasswordActualTrigger1"/>
                                </EventTrigger>
                            </ImageButton.Triggers>
                        </ImageButton>
                    </Grid>
                    
                    <Button Clicked="OnRegistrationClicked" Text="Submit" 
                            TextColor="WhiteSmoke"/>
                    
                    <StackLayout
                        Padding="20"
                        Margin="50,10"
                        IsVisible="True"
                        Orientation="Horizontal">
                        <Label Text="Have an account?"
                               HorizontalOptions="Center"
                               TextColor="DarkSlateGray"/>
                        <Label Text="Sign in"
                               TextColor="DodgerBlue">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="SignInTapped" />
                            </Label.GestureRecognizers>
                        </Label>
                        
                    </StackLayout>
                </StackLayout>

            </StackLayout>
        </FlexLayout>
    </Grid>

</ContentPage>