<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AeroArchive.Views.NewEmployeePage"
             Shell.PresentationMode="ModalAnimated"
             Title="New Employee"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" xmlns:model="clr-namespace:AeroArchive.Models" xmlns:viewmodels="clr-namespace:AeroArchive.ViewModels"
             ios:Page.UseSafeArea="true">
    <ContentPage.Content>
        <StackLayout Spacing="3" Padding="15">
            <Label Text="Employee Full Name" FontSize="Medium" />
            <Entry Text="{Binding FullName, Mode=TwoWay}" FontSize="Medium" />
            <Label Text="Employee ID" FontSize="Medium" />
            <Entry Text="{Binding EmployeeID, Mode=TwoWay}" FontSize="Medium" />
            <Label Text="Role" FontSize="Medium" />
            <Entry Text="{Binding Role, Mode=TwoWay}" FontSize="Medium" />
            <Label Text="Email" FontSize="Medium" />
            <Entry Text="{Binding Email, Mode=TwoWay}" FontSize="Medium" Margin="0" />
            <StackLayout Orientation="Horizontal" x:DataType="viewmodels:NewEmployeeViewModel">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
