<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewmodels="clr-namespace:AeroArchive.ViewModels" 
             x:DataType="viewmodels:EmployeeDetailViewModel"
             x:Class="AeroArchive.Views.EmployeeDetailPage"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit" Command="{Binding EditEmployeeCommand}" />
        <ToolbarItem Text="Delete" Command="{Binding DeleteEmployeeCommand}" />
    </ContentPage.ToolbarItems>

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Employee Name:" FontSize="Medium" />
        <Label Text="{Binding Fullname}" FontSize="Small"/>
        <Label Text="Employee ID:" FontSize="Medium" />
        <Label Text="{Binding EmployeeiD}" FontSize="Small"/>
        <Label Text="Role:" FontSize="Medium" />
        <Label Text="{Binding Role}" FontSize="Small"/>
        <Label Text="Email:" FontSize="Medium" />
        <Label Text="{Binding Email}" FontSize="Small"/>
    </StackLayout>

</ContentPage>